{
  "name": "online-shopping-hub",
  "version": "1.0.0",
  "description": "Full-stack E-Commerce Platform with React frontend and Node.js backend",
  "scripts": {
    "dev": "docker-compose up -d postgres && concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:start": "cd backend && npm start",
    "frontend:build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "./scripts/setup-db.sh",
    "test": "cd backend && npm test"
  },
  "keywords": [
    "ecommerce",
    "react",
    "nodejs",
    "postgresql",
    "fullstack"
  ],
  "author": "Dhvanil Patel",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}